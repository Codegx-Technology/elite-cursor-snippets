# Shujaa Studio - Example Environment Variables
# -------------------------------------------------
# Copy this file to .env and fill in real values locally.
# Never commit real secrets. Keep .env in .gitignore (already set).
# This file is the source of truth for required keys.

# Hugging Face API
HF_API_KEY=

# News API
NEWS_API_KEY=
# Google Custom Search (Optional for images)
GOOGLE_SEARCH_API_KEY=your_google_search_api_key
GOOGLE_CX=your_custom_search_engine_id

# YouTube Upload
# Path to your OAuth client secret JSON downloaded from Google Cloud Console
YOUTUBE_CLIENT_SECRET_FILE=client_secret.json

# TTS Model (default works out-of-the-box)
TTS_MODEL=facebook/mms-tts-eng

# Output Settings
VIDEO_OUTPUT_DIR=./output_videos

# Admin Authentication (Super Admin)
SUPERADMIN_USERNAME=peter
# IMPORTANT: change this locally; do not commit real secrets
SUPERADMIN_PASSWORD=please_change

# SMTP (Gmail) for Admin Notifications
# Use a Gmail App Password (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=solver.peters@gmail.com
SMTP_PASSWORD=your_gmail_app_password
SMTP_FROM=solver.peters@gmail.com
SMTP_TO=solver.peters@gmail.com
